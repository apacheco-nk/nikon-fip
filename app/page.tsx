/* oxlint-disable nextjs/no-img-element -- Las imágenes ya incluyen variantes optimizadas y logos SVG. */
'use client';
import { useState } from 'react';
import Link from 'next/link';
import { useRegistration, setRegistration } from '@/lib/registration';
import { ArrowRight, Check, CalendarDays, MapPin } from 'lucide-react';
import { Header, Footer } from '@/components/brand';
import { eventDays } from '@/lib/event';
import { galleries } from './gallery-data';
import { galleryUrl } from '@/lib/gallery-url';
import colorGalleryCover from '@/imgs/galeria.jpg';
import colorGalleryCoverDayTwo from '@/imgs/galeria2.jpg';
export default function Home() { const registered=useRegistration()==='registered'; const [submitting,setSubmitting]=useState(false); const [formError,setFormError]=useState(''); async function submit(formData:FormData){setSubmitting(true);setFormError('');const field=(name:string)=>{const value=formData.get(name);return typeof value==='string'?value:''};const lead=Object.fromEntries(['nombre','email','telefono','camara','instagram','website'].map(name=>[name,field(name)]));try{const response=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...lead,consentimiento:formData.get('consentimiento')==='on'})});const data=response.headers.get('content-type')?.includes('application/json')?await response.json():null;if(!response.ok||!data?.ok)throw new Error(data?.error||'No pudimos completar el registro. Inténtalo nuevamente.');setRegistration(true);setTimeout(()=>document.querySelector('#galerias')?.scrollIntoView({behavior:'smooth'}),50)}catch(error){setFormError(error instanceof Error?error.message:'No pudimos completar el registro.')}finally{setSubmitting(false)}} return <main><Header/><section className="hero"><div className="hero-copy"><img className="event-logo" src="/brand/foto-image.svg" alt="Expo / Fest FOTOIMAGE"/><p className="eyebrow">Nikon Foto Image · Perú</p><h1>Tu mirada.<br/><em>Tu fotografía.</em></h1><p className="hero-text">Si te fotografiaste con nuestro equipo Nikon en Foto Image, encuentra y descarga aquí tu retrato.</p><div className="event-details"><span><CalendarDays size={19}/>17–20 septiembre 2026</span><span><MapPin size={19}/>Lima, Perú</span></div></div><div className="lead-card">{registered?<div className="success-state"><span className="success-icon"><Check/></span><p className="eyebrow">Registro completo</p><h2>¡Gracias!</h2><p>Te avisaremos cuando las fotografías estén disponibles.</p><a className="primary-button" href="#galerias">Ver jornadas <ArrowRight size={18}/></a><button className="text-button" onClick={()=>setRegistration(false)}>Registrar a otra persona</button></div>:<form action={submit}><div className="form-heading"><span>01</span><div><p className="eyebrow">Antes de comenzar</p><h2>Cuéntanos de ti</h2></div></div><div className="field-grid"><label className="wide">Nombre completo<input name="nombre" autoComplete="name" maxLength={120} required placeholder="Ej. Andrea García"/></label><label>Correo electrónico<input name="email" type="email" autoComplete="email" maxLength={254} required placeholder="tu@email.com"/></label><label>Teléfono<input name="telefono" type="tel" autoComplete="tel" maxLength={40} required placeholder="+51 987 654 321"/></label><label>Modelo de cámara<input name="camara" maxLength={120} required placeholder="Ej. Nikon Z6III"/></label><label><span>Instagram <small>(opcional)</small></span><input name="instagram" autoComplete="off" maxLength={120} placeholder="@usuario"/></label><label className="honeypot" aria-hidden="true">Sitio web<input name="website" tabIndex={-1} autoComplete="off"/></label></div><label className="consent"><input name="consentimiento" type="checkbox" required/><span>Acepto recibir novedades de Nikon y el tratamiento de mis datos según la política de privacidad.</span></label>{formError&&<p className="form-error" role="alert">{formError}</p>}<button className="primary-button" type="submit" disabled={submitting}>{submitting?'Registrando…':'Encontrar mis fotografías'}{!submitting&&<ArrowRight size={18}/>}</button></form>}</div></section>{registered&&<section id="galerias" className="gallery-picker"><div className="section-heading"><p className="eyebrow">Galerías del evento</p><h2>Elige tu jornada</h2><p>Selecciona el día en que te fotografiamos.</p></div><div className="day-grid">{eventDays.map(day=>{const hasPhotos=galleries[day.slug].length>0;const content=<><div className="day-image"><img src={day.slug==='17-septiembre'?colorGalleryCover.src:day.slug==='18-septiembre'?colorGalleryCoverDayTwo.src:galleryUrl(day.cover)} alt=""/><span className="photo-count">{hasPhotos?galleries[day.slug].length+' fotos':'Próximamente'}</span></div><div className="day-caption"><small>{day.date}</small><h3>{day.day}</h3><span>{hasPhotos?'Ver jornada':'Galería no disponible'}{hasPhotos&&<ArrowRight size={18}/>}</span></div></>;return hasPhotos?<Link className="day-card" href={'/'+day.slug} key={day.slug} aria-label={day.day+' '+day.date+'. Ver galería'}>{content}</Link>:<div className="day-card day-card--pending" key={day.slug}>{content}</div>})}</div></section>}<Footer/></main> }
